
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.4
  creationTimestamp: null
  name: isovalentpodnetworks.isovalent.com
spec:
  group: isovalent.com
  names:
    categories:
    - cilium
    - isovalent
    kind: IsovalentPodNetwork
    listKind: IsovalentPodNetworkList
    plural: isovalentpodnetworks
    shortNames:
    - ipn
    singular: isovalentpodnetwork
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IsovalentPodNetwork defines a network to which pods can be attached.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PodNetworkSpec is the pod network specification.
            properties:
              ipam:
                description: IPAM is the IPAM specificatiojn for this network.
                properties:
                  mode:
                    description: Mode is the IPAM mode used for this network. Currently
                      only multi-pool is supported.
                    enum:
                    - multi-pool
                    type: string
                  pool:
                    description: Pool is the IPAM pool to use for this network.
                    properties:
                      name:
                        description: Name is the IPAM pool's name.
                        maxLength: 253
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    required:
                    - name
                    type: object
                required:
                - mode
                - pool
                type: object
              routes:
                description: Routes are the routing specifications for this network.
                items:
                  description: RouteSpec is the routing specification for a pod network.
                  properties:
                    destination:
                      description: Destination is the route's destination CIDR.
                      format: cidr
                      type: string
                    gateway:
                      description: Gateway is the route's gateway IP address.
                      format: ip
                      type: string
                  required:
                  - destination
                  type: object
                type: array
            required:
            - ipam
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
